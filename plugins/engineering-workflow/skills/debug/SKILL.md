---
name: debug
description: 用可复现反馈回路定位 Bug、性能回退或间歇性故障的根因，并在授权时补回归测试和修复。当用户报告异常、崩溃、错误结果、卡顿、资源泄漏，或明确要求 debug、诊断、排查时使用；不能复现时先建立证据链，不要猜答案。
---

# 诊断问题

没有能识别用户真实症状的反馈命令，就不能宣称已经定位根因。

## 诊断闭环

1. 把症状变成可重复的测试、回放、日志查询、基准或固定输入。
2. 确认当前版本确实失败，并记录最小必要输出。
3. 缩小输入、代码路径、时间窗口或环境差异，保留真实症状。
4. 在信息允许时提出多个可证伪假设，按区分度和成本排序。
5. 每次只改变一个变量；用临时插桩或对照实验排除假设。
6. 追踪根因所在的共享函数、状态边界和所有调用方，不停在首个异常栈表面。
7. 先添加能复现问题的最小回归检查，再在用户已授权修复时修改代码。
8. 用原始反馈回路和扩大测试共同验证修复，并移除临时插桩。

## 边界

- 用户只要求诊断时，停在根因、证据和建议修复，不实施改动。
- 无法复现时，报告已排除内容、缺失证据和下一条最有价值的观测，不编造确定结论。
- 性能问题必须有前后可比较的测量；单次体感不是证据。
- 真实硬件、时钟和传感器允许校准差异；不要用理论常数掩盖物理偏差。

## 完成输出

给出：复现方式、根因、排除过的主要假设、回归检查、修复验证和仍未覆盖的环境风险。
