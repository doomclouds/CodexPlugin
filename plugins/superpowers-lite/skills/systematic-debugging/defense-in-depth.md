# 纵深防御验证

只有当独立保护覆盖不同信任边界或故障模式时，才增加纵深防御。例如：API 边界验证不可信输入、业务逻辑维护不变量、测试环境保护破坏性操作。不要为了凑层数而增加保护。

从已观察到的故障路径出发，找出同一无效状态可能进入、绕过检查或造成不成比例损害的边界。在这些边界添加最小且有用的保护，再用最强适用的反事实或回读逐个验证。

示例：入口边界可以拒绝空项目目录；如果其他调用者绕过入口，工作区不变量可以再次拒绝；测试专用保护可以拒绝在临时存储之外执行破坏性操作。这些是针对不同故障模式的独立保护，不是强制的层级序列。
