# Deprecated Plugins

这里存放暂时不用、但还想保留源码的插件。

这些插件不会出现在当前 `.agents/plugins/marketplace.json` 中，也不应作为当前 `plugins/` 目录下的可安装插件发布。

当前弃用：

- `drawio`: 暂时不用的 draw.io 图表插件。
- `superpowers-asset-compounding`: 暂时不用的 Superpowers 资产复利插件。
